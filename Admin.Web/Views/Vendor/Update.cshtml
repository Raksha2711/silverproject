@model Command.Entity1.Vendor


<section class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6">
                <h1>Create Vendor</h1>
            </div>
            <div class="col-sm-6">
                <ol class="breadcrumb float-sm-right">
                    <li class="breadcrumb-item"><a href="#">Home</a></li>
                    <li class="breadcrumb-item active">Create Vendor</li>
                </ol>
            </div>
        </div>
    </div><!-- /.container-fluid -->
</section>
<div class="content">
    <div class="container-fluid">
        <div class="card card-default">
            <form asp-controller="vendor" asp-action="update" method="post">
                <div class="card-header">
                    <h3 class="card-title">Vendor</h3>
                </div>
                <!-- /.card-header -->
                <div class="card-body">
                    @Html.HiddenFor(m => m.Id)
                    @Html.HiddenFor(m => m.ModifiedBy)
                    @Html.HiddenFor(m => m.ModifiedDate)
                    <div class="row">
                        <div class="col-md-6">
                            <div class="form-group">
                                <label>Name</label>
                                @Html.TextBoxFor(m => m.Name, new { @class = "form-control", placeholder = "Enter Name" })
                            </div>

                        </div>
                        <div class="col-md-6">
                            <div class="form-group">
                                <label>Address</label>
                                @Html.TextBoxFor(m => m.Address, new { @class = "form-control", placeholder = "Enter Address" })
                            </div>

                        </div>
                    </div>
                    <div class="row">
                        <div class="col-md-6">
                            <div class="form-group">
                                <label>EmailId</label>
                                @Html.TextBoxFor(m => m.EmailId, new { @class = "form-control", placeholder = "Enter EmailId" })
                            </div>

                        </div>
                        <div class="col-md-6">
                            <div class="form-group">
                                <label>EmailId(Optional)</label>
                                @Html.TextBoxFor(m => m.EmailId2, new { @class = "form-control", placeholder = "Enter EmailId" })
                            </div>

                        </div>
                        <div class="col-md-6">
                            <div class="form-group">
                                <label>ContactNo</label>
                                @Html.TextBoxFor(m => m.ContactNo, new { @class = "form-control", placeholder = "Enter ContactNo" })
                            </div>

                        </div>
                        
                        <div class="col-md-6">
                            <div class="form-group">
                                <label>Contact Person</label>
                                @Html.TextBoxFor(m => m.ContactPerson, new { @class = "form-control", placeholder = "Enter Contact Person" })
                            </div>

                        </div>
                    </div>
                </div>
                <!-- /.card-body -->
                <div class="card-footer">
                    <div class="card-tools">
                        <button type="submit" class="btn btn-primary"><i class="fas fa-save"></i>Save</button>
                        <button type="submit" class="btn btn-primary" asp-controller="vendor" asp-action="index"><i class="fas fa-times"></i>Cancel</button>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>

